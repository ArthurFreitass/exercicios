// Área para declaração de variáveis

let sum = 1;
let factorial = 1;
let factorial_1 = 0;

// Loop de repetição

for (let index = 1; index <= 10; index += 1) {
  sum = sum * index;
}

// Saída

console.log(`O fatorial de 10 é ${sum}`);
