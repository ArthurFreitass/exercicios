// Implemente uma função que calcule a média de três números fornecidos pelo usuário.

function Average(number1, number2, number3) {
  let sum = number1 + number2 + number3;
  return "A média entre os três números é: " + sum / 3;
}

console.log(Average(10, 10, 5));
