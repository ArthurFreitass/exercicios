// Crie uma função que recebe a base e a altura de um triângulo e retorna a sua área.

const width = 12.5;
const heigth = 4;

function AreaTriangulo(x, y) {
  return "A área do triângulo é " + (x * y) / 2;
}

console.log(AreaTriangulo(width, heigth));
