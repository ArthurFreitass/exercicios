const pessoa = {
  Nome: "João",
  Idade: 21,
};

const keys = Object.keys(pessoa);

console.log(keys);

const values = Object.values(pessoa);

const entries = Object.entries(pessoa);

console.log(values);

console.log(entries);
